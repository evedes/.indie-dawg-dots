# Chromium browser profile data (runtime/cache) — keep only the launch flags config
archlinux/chromium/*
!archlinux/chromium/chromium-flags.conf
docs/*

# Commercial fonts — licensed, never commit (kept locally in ~/Downloads/fonts)
BerkeleyMono*
