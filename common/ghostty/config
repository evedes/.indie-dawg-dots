# Platform-aware Ghostty configuration
# This loads common settings and the appropriate platform-specific settings
# Note: Ghostty will ignore config files that don't exist

# Load common configuration first
config-file = ~/.indie-dawg-dots/common/ghostty/config.common

# Platform-specific configurations
# Uncomment the line for your current platform:
# config-file = ~/.indie-dawg-dots/common/ghostty/config.macos
config-file = ~/.indie-dawg-dots/common/ghostty/config.linux

keybind = shift+enter=text:\n
