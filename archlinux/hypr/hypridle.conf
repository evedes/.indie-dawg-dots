general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    inhibit_sleep = 3
}

# Lock after 10 minutes without powering down displays or suspending the host.
listener {
    timeout = 600
    on-timeout = loginctl lock-session
}
