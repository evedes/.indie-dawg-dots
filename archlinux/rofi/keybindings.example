# Additional Hyprland keybindings for Rofi features
# Add these lines to your ~/.config/hypr/keybindings.conf file
#
# Note: The main launcher is already configured as:
# bind = $mainMod, SPACE, exec, $menu
# where $menu is defined in applications.conf

# === Additional Rofi Modes ===

# Window switcher (Alt+Tab style)
bind = ALT, Tab, exec, rofi -show window -theme ~/.config/rofi/hexarchy-theme.rasi

# Run command (dmenu replacement)
bind = $mainMod, D, exec, rofi -show run -theme ~/.config/rofi/hexarchy-theme.rasi

# File browser
bind = $mainMod, E, exec, rofi -show filebrowser -theme ~/.config/rofi/hexarchy-theme.rasi

# === Direct Screenshot Bindings ===
# These bypass the menu for quick access

# Region screenshot
bind = , Print, exec, hyprshot -m region -o ~/Pictures/Screenshots

# Window screenshot
bind = SHIFT, Print, exec, hyprshot -m window -o ~/Pictures/Screenshots

# Fullscreen screenshot
bind = CTRL, Print, exec, hyprshot -m output -o ~/Pictures/Screenshots

# Region to clipboard
bind = $mainMod, Print, exec, hyprshot -m region --clipboard-only

# Window to clipboard
bind = $mainMod SHIFT, Print, exec, hyprshot -m window --clipboard-only

# === Quick Actions ===

# Lock screen
bind = $mainMod, L, exec, hyprlock

# Clipboard history (requires cliphist to be running)
bind = $mainMod, C, exec, cliphist list | rofi -dmenu -theme ~/.config/rofi/hexarchy-theme.rasi -p "Clipboard" | cliphist decode | wl-copy

# Color picker
bind = $mainMod SHIFT, C, exec, hyprpicker | wl-copy && notify-send "Color copied to clipboard"