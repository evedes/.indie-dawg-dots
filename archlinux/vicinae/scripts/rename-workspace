#!/usr/bin/env bash
# @vicinae.schemaVersion 1
# @vicinae.title Rename Workspace
# @vicinae.mode silent
# @vicinae.icon 🏷️
# @vicinae.keywords ["workspace", "rename", "name", "label", "hyprland"]
# @vicinae.description Give the active workspace a custom name (leave empty to clear)
# @vicinae.argument1 { "type": "text", "placeholder": "Name", "optional": true }

exec "$HOME/.indie-dawg-dots/archlinux/bin/workspace-stack" rename "${1:-}"
